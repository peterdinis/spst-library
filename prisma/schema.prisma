// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Book {
    id           Int     @id @default(autoincrement())
    name         String
    description  String
    image        String
    authorName   String
    year         Int
    pages        Int
    isAvaiable   Boolean
    itemsInStock Int
    categoryNmae String  @default("ABC")
}

model Category {
    id          Int    @id @default(autoincrement())
    name        String
    description String
}

model Publisher {
    id   Int    @id @default(autoincrement())
    name String
}

model Author {
    id   Int    @id @default(autoincrement())
    name String
    deathYear String?
    birthYear String
    description String
    litPeriod String
    totalBooks Int
    authorImage String @default("")
}